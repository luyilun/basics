class Str {
  /**
   * 去除全部空格
   * @param val
   */
  static strip = function (val: string) {};
  /**
   * 去除两边空格
   * @param val
   */
  static trim = function (val: string) {};
}
export default Str;
